# Chapter validation status

Boundary validation runs the published HCM example problems through the built WASM package (`tests/boundary/`, `node tests/boundary/<file>.mjs`), asserting the same expected values and tolerances as the Rust core tests in `transportations-library/tests/`. Core = Rust integration tests against the book. Boundary = the same numbers reproduced through the JS constructor path the calculator uses. A chapter leaves beta when it passes both layers and Rei has inspected it.

Inspection workflow: go chapter by chapter, read the test file, spot-check the expected values against the cited HCM example problem, run the file, then mark the sign-off line. Decision items collect anything that needs a judgment call rather than a code fix.

| Ch | Method | Core EPs | Boundary checks | Status | Sign-off |
|----|--------|----------|----------------|--------|----------|
| 10 | Freeway Facilities | Ch.25 EP1, EP2, EP3, EP4, EP5, EP6 | 1592 | pass | |
| 11 | Freeway Reliability | Ch.25 EP7 (full fidelity), EP8, EP9 | 189 | pass | |
| 12 | Basic Freeway Segments | Ch.26 EP1-3 | 18 | pass (after f_HV fix) | |
| 12 | Managed Lanes | Ch.26 EP7 | 51 | pass | |
| 13 | Weaving Segments | Ch.27 EP1-3 | 53 | pass | |
| 14 | Merge and Diverge | Ch.28 EP1-5 | 75 | pass | |
| 15 | Two-Lane Highways | 4 fixture cases | 168 | pass | |
| 16 | Urban Street Facilities | Ch.29 §5 EP1 EB+WB | 55 | pass | |
| 17 | Urban Street Reliability | Ch.29 §5 EP4 + EP5 Strategy 1, Ch.37 §5 ASC | 37 | pass | |
| 18 | Urban Street Segments | Ch.30 §8 EP1 (all three AP-delay paths) | 72 | pass | |
| 19 | Signalized Intersections | Ch.31 §10 EP1 + Exhibit 31-7 | 155 | pass | |
| 20 | TWSC | Ch.32 EP1, EP2 (pedestrian mode), EP3 | 112 | pass | |
| 21 | AWSC | Ch.32 EP1-2 | 34 | pass | |
| 22 | Roundabouts | Ch.33 EP1-2 | 55 | pass | |
| 23 | Ramp Terminals | Ch.34 EP1, EP2, EP5, EP7, EP12, EP13, EP14, EP15, EP16 | 551 | pass | |
| 24 | Off-Street Ped/Bike | Ch.35 EP1-2 | 15 | pass | |
| 25/26 | Mixed-Flow Model | Ch.26 EP5 (mixed-flow half), Ch.25 EP11 | 88 | pass, no page | |

## Findings from the boundary pass

**Chapter 12 f_HV defect (fixed in `wasm_basicfreeways.rs`).** The binding never set `sut_percentage`, the core defaults it to 50, which routes the heavy-vehicle adjustment through the sparse mixed-flow SUT table and silently degrades f_HV to 1/(1 - p_t) off the table grid. Densities and LOS were wrong for freight scenarios (EP1 published LOS C, page showed B). The binding now defaults `sut_percentage = 0` (terrain-based Exhibit 12-25 E_T, the convention of the published EPs and the Rust tests) and exposes `sut_percentage` and `e_t` as trailing optional constructor args for mixed-flow analysis.

**Middleware 0.3.5 and 0.3.6 / library 0.3.1 landed three HCM-text corrections (2026-08-10).** Two of them move numbers this calculator checks or prints, and one of those moves away from a published value that the library adjudicated as a book defect, so they are recorded here rather than in a test comment alone.

1. *Interchange-level LOS comes from the demand-weighted ETT alone.* Chapter 23 Step 9 no longer propagates the per-O-D v/c and R_Q flags into the aggregate letter. Exhibit 23-10's "automatically LOS F" rule is a per-O-D rule and the per-O-D behavior is unchanged. Nothing in the boundary suite or the pages asserted a flag-forced aggregate, so no expectation moved for this one.
2. *Incremental delay d2 uses the Step 7 lane group capacity, not the per-lane capacity*, per the Equation 19-26 variable list. This is the one that moves printed numbers. Chapter 34 Example Problem 1 is the single worked example whose published d2 reproduces only per-lane, so its worksheet is treated as a book defect outvoted by the equation text and by Example Problems 3 and 5. The hcm23 page's default (EP1) interchange ETT prints 50.4 s/veh where it printed 52.4 and where Exhibit 34-16 publishes 52.4, with LOS C unchanged and every O-D letter still matching the published one. The DDI form (EP5) prints 29.8 s/veh and LOS B where it printed 34.8 and C, against a published 34.9 and C. The Exhibit 23-10 B/C boundary is 30 s/veh, so that aggregate now sits 0.2 s/veh on the far side of a band edge. It is carried by the westbound O-Ds, whose Exhibit 34-64 movement delays the library already documents as not reproducible from the printed equations, and the same correction is what makes EP5's O-D E reproduce its published 24.7 s/veh and LOS B exactly.
3. *Two-lane facility follower density aggregates the adjusted segment densities* (Equation 15-39), centralized in `TwoLaneHighways::determine_facility_follower_density`. **Reached the calculator in middleware 0.3.6** (2026-08-10), where the aggregator is bound as `WasmTwoLaneHighways.determine_facility_follower_density()` and both the hcm15 page and the ch15 boundary suite now call it. The correction moved the boundary suite and did not move the page. The suite had been weighting the per-segment column itself, taking fd_mid on a passing lane and the raw fd everywhere else, which discards the Step 9 downstream passing-lane benefit: case1 and case2 agreed, case3 (Chapter 26 Example Problem 3) gave 8.041 followers/mi and LOS D where Equation 15-39 gives 7.271 and LOS C and Exhibit 26-27 publishes 7.3 and LOS C, and case4 (Example Problem 4) gave 20.219 against 19.897 while staying inside the LOS E band and so breaking no assertion. Its Step 11 expectations move accordingly, and it now pins the facility density itself rather than only the letter it produces.

   **Correction to the note this replaces**, which said the page reweighted the column the same way. It did not. `src/routes/hcm15/+page.svelte` already selected the adjusted density per segment, substituting fd_mid on a passing lane and the Step 9 fd_adj wherever one applied, which is what Equation 15-39 asks for. Measured over all four fixtures against the bound aggregator, the page's value agrees to the printed precision on every one, so no page expectation and no Playwright pin moved. The page now calls the aggregator regardless, because keeping a second implementation of a computation the library centralized is what let this drift start.

**Chapter 20 gains the Section 5 pedestrian mode (2026-08-13).** Library 0.3.2 added it and nothing downstream could reach it. Middleware 0.3.9 binds it as `WasmPedestrianCrossing`, which takes the crossing as one serde config object and returns the whole analysis, and the hcm20 page gains a mode selector between the vehicular Section 4 method it already had and the new Section 5 one. The two are different procedures, not two views of one: on the vehicular page pedestrian volumes are an impedance that reduces vehicular capacity, and here the pedestrian is the subject and LOS comes from the proportion who would rate the crossing "dissatisfied" or worse (Exhibit 20-3) rather than from any delay. The page says so, because a printed 6.0 s of pedestrian delay next to a LOS C reads like a contradiction otherwise.

The ch20 boundary file gains Chapter 32 Example Problem 2 as a three-scenario block over `case4_pedestrian.json`, taking the file from 36 to 112 checks. All three scenarios reproduce at the core's own tolerances: Scenario A at 760.6 s and LOS F against a published 761 and F, Scenario B at 5.96 s and LOS C against 6.0 and C, and Scenario C at 2.94 s and LOS A against 3.0 and A, with the Exhibit 32-7 satisfaction rows, the published P(Y_i) cumulative sums, and the countermeasure progression from F to C to A. The Playwright suite pins the same three totals and letters through the page.

Five things about this method are documented rather than matched, and they are the library's rather than the binding's. Two are equations the HCM 7th Edition PDF clips inside a horizontally scrollable typeset box. **Equation 20-95's I_MR and I_NY coefficients appear nowhere in the Chapter 20 text**, and were recovered by least squares against the six published O(S/D) values of this same example problem, which over-determine the two unknowns. That makes the satisfaction rows of the boundary block a fit residual rather than an independent reproduction, and the boundary file labels them that way. It also means the pedestrian mode's LOS rests on two constants with no published source, which is the one decision item here rather than a code fix: **VERIFY-HCM, replace both coefficients if an uncut copy of Equation 20-95 becomes available.** Equations 20-91 and 20-92 are clipped by the same typesetting but are fully recoverable from the chapter itself, so they carry no such risk. The other three are the engine evaluating the yield probability as one binomial sum over blocked lanes rather than the book's four transcribed lane cases, which extends past the four-lane crossings the HCM enumerates; N_p carried as a real number where the text calls it a count of pedestrian rows, which is what Equation 20-77 actually produces; and Step 7 taking the first stage's P_d and P(Y_1) on a two-stage crossing, which Example Problem 2 cannot settle because its two stages are identical by construction, though it does rule out the across-stage product by publishing P_nd = 0.481 rather than 0.481 squared.

**Publish gate, resolved 2026-08-13.** This chapter's boundary block and the pedestrian mode of the hcm20 page run against `crosstraffic_middleware` 0.3.9, published to crates.io on 2026-08-13 tracking `transportations_library` 0.3.4 (also published). A clean-clone `npm run wasm` resolves.

**Chapter 23 gains the Parclo A-2Q, the first interchange here that is not the diamond skeleton (2026-08-12).** Library 0.3.3 replaced the closed ten-variant `InterchangeMovement` with a composition of approach, position, and turn, which is what allowed the lane groups a parclo actually has to be named: an external left onto the loop quadrant and an internal shared through-and-right, with no internal left at all. Middleware 0.3.8 tracks it. Almost nothing needed binding, because `WasmInterchange` takes the whole interchange through its serde schema, so the composed names, the six parclo form names, and the new per-movement `design_speed_mph` all arrived on their own; the one addition is `get_form()`, a readback the boundary suite uses to prove the form it sent is the form that ran.

The ch23 boundary file gains Chapter 34 Example Problem 2 as a full-fixture block over `case6.json` (I-75 at Newberry Avenue, C = 140 s, PHF 0.95, D = 800 ft), taking the file from 257 to 381 checks. All ten Exhibit 34-24 through 34-26 effective greens are exact, the eight non-internal capacities land within 2 veh/h of the published values, and every Exhibit 34-29 O-D LOS letter and both O-D flags are exact. The interchange reads 61.5 s/veh and LOS D against a published 61.3 and D. Three things about the example are documented rather than matched, mirroring the library: Exhibit 34-22's internal lane utilization of 1.000 against Chapter 19's 0.908, which Examples 1, 3, and 4 all print and which gives a better O-D table here (mean absolute error 0.26 against 0.63 s/veh over the ten published ETTs); Exhibit 34-25's printed 1,282 veh/h for the eastbound internal group against the 1,356 that both Exhibit 34-27's own v/c and Exhibit 34-25's own arrival rate imply; and Exhibit 34-20's rightmost-lane shares, which need an Exhibit 23-24 coefficient of 0.655 where the exhibit prints 0.605, so the fixture supplies the published factors as overrides.

On the page, the hcm23 Part B form selector gains a Parclo A-2Q entry loading that example as its defaults, with its own `ParcloDiagram` plan view. The other five Exhibit 23-17 parclos and the Exhibit 23-18 SPUI route and analyze in the engine but have no published answer column behind them, so they are deliberately not offered and the page's note says so rather than leaving the absence unexplained. **Decision item for sign-off:** whether to expose those five as an explicitly unvalidated group later, or keep the selector to forms with a published example.

**~~Noticed and left alone~~ — settled upstream, and the fixture was right.** This entry previously flagged the `case6.json` external left bays as 200 ft where the Exhibit 34-17 plan view appears to dimension them at 500, and said it belonged in the library rather than here. The library adjudicated it on 2026-08-13 and recorded the evidence in the fixture: Exhibit 34-25 prints the storage length it used as its own row, 800 / 200 / 800 ft on both arterial approaches, and the published R_Q row of 1.96 eastbound and 0.78 westbound is what 200 ft gives (the engine reads 1.960 and 0.781 on those inputs). Read as 500 ft, the westbound R_Q falls to 0.312, which agrees with nothing published. The 500 came from the plan view, which is drawn to no scale and whose handwritten bay dimensions are unreadable at any rendering. No number in this repo moves and nothing here needs changing; the entry stands corrected so it is not re-opened from the drawing a third time.

**Chapter 23 gains the single-point urban interchange, the first form here with no internal link (2026-08-13).** Library 0.3.4 added protected-plus-permitted left turns to the Chapter 23 pipeline, and middleware 0.3.9 tracks it. As with the parclo the release before, nothing needed binding: `WasmInterchange` takes the whole interchange through its serde schema, so the optional `protected_permitted_left` object on a lane group deserializes on arrival and the two new `protected_sat_flow` and `permitted_sat_flow` result fields serialize back out. `Spui` was already a form name in 0.3.3 and `get_form()` already read it back, so the only source change upstream was a doc comment. The shim's `crosstraffic_middleware` pin moves to 0.3.9 and `pkg/` is rebuilt.

The ch23 boundary file gains Chapter 34 Example Problem 7 as a full-fixture block over `case7.json` (I-95 at University Drive, C = 110 s, PHF 0.95, a pretimed three-phase SPUI), taking the file from 381 to 551 checks and the 16-file suite from 2,904 to 3,074. The property worth pinning is the single-point convention itself, and it is visible from the results alone: every O-D resolves onto exactly one lane group, so every EDTT is exactly zero and every ETT equals that movement's control delay to the last bit, which is what Exhibit 34-82 shows by printing its ETT column equal to the Exhibit 34-80/34-81 delays. All ten effective greens are exact, including the 48 s the two arterial lefts get from summing their 16 s protected and 32 s permitted windows, and the recombination identity holds to 1e-9: the collapsed capacity is the sum of the two component capacities and the collapsed saturation flow is that sum re-expressed over the summed green. The interchange reads 45.35 s/veh and LOS C against a published 48.3 and C, and eight of the ten published Exhibit 34-82 O-D LOS letters are exact.

**The two O-D letters that differ, and the seven book defects behind them.** O-D D reads C where the book reads D and O-D E reads B where the book reads C; they are the two sitting closest to an Exhibit 23-10 band edge, D running 5.2 s/veh short of the 55 s C/D line and E 1.8 s/veh short of the 30 s B/C line. Every defect is in the saturation flow chain and every one is the library's, mirrored here rather than matched. Exhibits 34-75 and 34-76 print a lane width factor of 0.967 for the stated 10.3 ft lanes; HCM 7's Exhibit 19-20 is a three-tier lookup returning 1.000 for every width from 10.0 to 12.9 ft, and 0.967 is the HCM 2000 continuous form evaluated at 11 ft. Exhibit 34-76 prints a heavy-vehicle-and-grade factor of 1.000 on the northbound and southbound approaches, which the example's own text gives 5% heavy vehicles and Equation 19-10 gives 0.961. The traffic pressure row reproduces Equation 23-15 exactly for the eight columns that are not a left-turn phase component and for none of the four that are, and it prints different values for the protected and permitted halves of one movement at one demand, which the equation cannot do. Exhibit 34-77 then contradicts itself three ways: its own g_u = G_perm − g_q − l_1 relation closes eastbound and misses westbound by 1.8 s, so the fixture carries g_u rather than g_q; it prints a westbound uniform delay of 22.7 s/veh where Exhibit 34-80's own d = d1 + d2 sum needs 22.8; and it labels its queue breakpoints in feet where Chapter 31 defines every one of them in vehicles. Exhibit 34-78 omits the Equation 31-124 sneaker term from the left-turn capacity, worth about 65 veh/h, which the engine omits too because Chapter 34 does. The first two nearly cancel on the ramp approaches, which is why those saturation flows land within 1% of the published column while the arterial ones run about 3.5% high.

**A distinction worth keeping straight in the numbers.** Exhibit 34-78's published saturation flows of 672 and 661 veh/h are reproduced by the recombination identity fed with the exhibit's *own* Exhibit 34-75 component flows, 1,560 and 561 eastbound and 1,561 and 573 westbound. They are not what the engine's own components give: it computes 714.5 and 697.5 from components of 1,659.5 / 595.1 and 1,665.1 / 580.5, for exactly the adjustment-factor reasons above. Both claims are asserted separately in the boundary block so neither can be read as the other.

On the page, the hcm23 Part B form selector gains a SPUI entry loading Example Problem 7 as its defaults, with a new `SpuiDiagram` plan view. Calculate prints an interchange ETT of 45.4 s/veh and LOS C with every EDTT column at 0.0. Two page-level details are worth recording. The intersection spacing field is withheld for this form rather than shown at zero, because its `min="100"` would make the form fail HTML validation and Calculate would silently do nothing. And the permitted phase's opposing flow is derived from the other direction's through demand rather than typed in, so editing a through demand cannot leave it stale; measured against the fixture's own 881 and 911 veh/h, the page's derived values reproduce the fixture answer to 0.004 s/veh on the worst O-D and 0.0002 on the interchange ETT.

**Decision item for sign-off:** the note on the page previously listed the SPUI alongside the five unvalidated parclos. It now lists only the five. The earlier decision item, whether to expose those five as an explicitly unvalidated group, is unchanged.

**Noticed and left alone:** `transportations-library/docs/hcm/VERIFICATION.md` item 12 still says Example Problem 7 "needs the Chapter 19 protected-plus-permitted left-turn surface, which this module does not model", which library 0.3.4 now does, and still counts the SPUI among the forms with no published answer column. That is a library-side record, not this repo's.

**Library PR #75 scoped the Equation 25-12 front-clearing test to a restored bottleneck, and Chapter 10 is re-anchored on it (2026-08-11).** The oversaturated engine was clearing the front of a queue on a test that did not require the bottleneck to have recovered. The correction moves the Example Problem 2 period-4 row, and the ch10 boundary suite is rebuilt against the library's re-anchored `chapter10_integration.rs` rather than carrying its own account of which cells reproduce.

Only one asserted boundary cell actually changed value: Example Problem 2, Segment 4, period 4 now reads the published LOS E where it read D. Measured both ways, the pre-#75 `pkg/` passes the whole old 618-check file *and* passes 973 of the new 974 checks, so the old file was blind to the correction and this one is not. The other additions are coverage the old file lacked rather than values that moved: Example Problem 2's period-4 densities for Segments 8-11 (40.4/40.4/38.2/34.8, Exhibit 25-58) reproduce their published values on both engines and are now asserted, as is the full Example Problem 3.

**The residual Example Problem 2 deviation, restated post-fix.** The engine still holds the period-4 residual queue in Segments 5-6 where the published FREEVAL run spills it back into Segments 1-4. What survives the correction: period-4 Segments 1-3 compute LOS D against a published E and Segment 5 computes E against a published D; period-4 speeds for Segments 1-6 compute 59.5/53.0/58.2/52.2/48.5/21.5 against a published 47.2/47.5/51.5/48.3/56.5/24.7; and period-3 Segment 5 computes 44.0 mi/h against a published 45.3. Segment 4's letter left this list with #75, and Segments 3-5 of the period-4 speed row moved a little toward the book (58.3/53.9/48.2 before) without closing. The facility aggregates carry it as an overall 49.30 mi/h and 36.53 veh/mi/ln against a published 50.5 and 35.6; those two are now pinned at the measured values to +-0.05 rather than the Rust file's +-1.5, because a pin exists to fail when it moves and +-1.5 would swallow a real regression. Every per-period facility LOS letter still matches the book.

**One Playwright page pin moved with the correction, on hcm11 rather than hcm10.** The Chapter 11 seeded whole-year scenario set prints Reliability Rating 62.8% where it pinned 62.9%; verified as engine-driven by re-running that one test against the pre-#75 `pkg/`, which still gives 62.9. The rating is the share of observations under a TTI threshold, so it is the metric a few reclassified scenarios can move, and mean TTI, the median, and PTI are unchanged to three decimals on the same run. The hcm10 page is EP1-based and undersaturated, so the correction cannot reach it; verified rather than assumed, all hcm10 page tests pass unchanged against the rebuilt package.

**Chapter 10 is complete at the boundary as of middleware 0.3.7 (2026-08-11), and what came through with it is two documented reproduction gaps rather than clean matrices.** Both belong to the core and neither is introduced by the binding, but both are now asserted here rather than only in Rust, so they will fail if they move.

Example Problem 4's work zone segment itself reproduces exactly. Segment 11 holds 50.2 mi/h and 37.1 veh/mi/ln in every period against a published 50.4-50.5 and 36.8-36.9, its capacity is 4,014 veh/h (the Exhibit 25-71 pre-CAF 4,499 times the Equation 10-11 CAF_wz of 0.892), and its period-1 demand-to-capacity ratio is 1.2567 against the published 1.26. All 55 cells of the Exhibit 25-72 d/c matrix and all 55 letters of the Exhibit 25-76 LOS matrix reproduce. The gap is in the queued segments upstream of the work zone, where the engine distributes the stored demand differently from the published FREEVAL run: 22 of the 55 Exhibit 25-73 volume-served cells, 21 of the 55 Exhibit 25-74 speed cells, and 40 of the 55 Exhibit 25-75 density cells are pinned at the engine value with the published one named at the assertion, which is the same split the Rust file records. The worst cells are period 5 Segment 3 at 12.4 mi/h against a published 18.6 and 102.1 veh/mi/ln against 73.5. That every LOS letter still lands where the book puts it says the disagreement is in how the total queue is distributed, not in its size. The facility aggregates carry it as an overall 16.2 mi/h against a published 19.5, with the overall density close at 81.6 against 80.5; both are pinned at the measured value to +-0.1 rather than at a band that would swallow a regression.

Example Problem 5 reproduces on every published cell except one. Exhibit 25-81 ML capacity 1,614 veh/h, the Exhibit 25-82 vd/c row, the Exhibit 25-83 friction-affected ML speeds of 53.5 and 52.1 mi/h with the friction flag asserted where it is and is not active, the Exhibit 25-84 GP density matrix, the Exhibit 25-86 lane groups, and the Exhibit 25-87 facility LOS of C/D/D/C/C all land. The exception is the Exhibit 25-87 period-3 combined density, 28.3 veh/mi/ln against a published 29.1. That is the VERIFY-HCM item the core documents: 28.3 is the exact Equation 10-1 lane-mile weighting of the book's own Exhibit 25-86 group densities (31.0 GP, 20.0 ML) and 29.1 is not derivable from them under that equation. LOS D is unaffected either way.

**Chapter 11 is re-anchored on the full Example Problem 7 fixture (2026-08-11).** Everything the fixture sets now reaches the core, so the Exhibit 25-104 distribution the boundary produces is the same experiment the Rust test runs rather than a milder one, and the metrics are asserted at the Rust test's values and tolerances with the published number named in each label. What reproduces: the 27 expected weather events of Equation 25-76, two heavy rain events in each summer month and one in every other with every snow, cold, and visibility type rounding to zero; the Exhibit 25-100 July Friday DAF of 1.3357, where the Exhibit 11-18 default gave 1.3388; the Exhibit 25-103 monthly incident frequencies at the Rust file's own tolerances rather than the widened ones this file used to carry; and TTI_50 1.033 against a published 1.03, TTI_mean 1.323 against 1.30, misery index 5.63 against 5.76, and semi-standard deviation 1.96 against 2.05. What does not reproduce is the tail, for the reason the core documents: the published values come from FREEVAL's Monte Carlo stream at seed 1, which this implementation cannot replay. PTI computes 1.97 against a published 1.67, TTI_max 39.7 against 33.57, the VMT-weighted reliability rating 84.5% against 90.8%, and the share above TTI 2 is 4.8% against a published 2.95% of VMT. Those four are pinned at the computed value with the published one named, exactly as the Rust file pins them.

The October and November incident frequencies are asserted equal to within 1e-12, which is the book defect the core records. Exhibit 25-100 prints identical demand-ratio rows for the two months, so identical frequencies follow from the published inputs, and the published October 0.83 against November 0.79 does not.

The old fifteen-argument call is kept as a labeled regression anchor, and it doubles as the control that the new inputs bite: it must model no weather, must produce the Exhibit 11-18 DAF of 1.3388, and must be measurably milder than the full-fidelity run in both mean TTI and misery index. Measured both ways, the pre-0.3.7 `pkg/` passes the whole old 61-check file, so the old anchor was blind to all three gaps, and the new file refuses to run against it at all, naming the eleven missing methods rather than throwing. The four shape-validation checks are new coverage rather than a moved value: they assert that an 11-month demand table, a Monday-to-Friday-only one, a 4-month weather matrix, and a short duration vector are each rejected instead of silently defaulted.

**A measured control, not an assumption, backs the claim that these matrices have teeth.** Removing the one `set_work_zone` call from the boundary file's segment builder and re-running fails 166 of the 1,592 checks. Separately, a full dump of every cell the 0.3.6 surface could read for Example Problems 1, 2, and 3 is byte-identical between a 0.3.6 build and a 0.3.7 build, so the new input changed nothing that did not ask for it.

**Both Chapter 10 capabilities are now on the hcm10 page as well as at the boundary (2026-08-11), so nothing bound for this chapter is page-only reachable through the boundary suite.** The work zone is an opt-in panel per segment, opened on the case4.json Segment 11 closure and never placed implicitly, because `set_work_zone({})` installs a real three-to-two closure rather than a no-op. The managed lane is an opt-in facility-level section driving `WasmManagedLaneFacility.from_gp`, defaulting to the ml_case1.json cross section, and the strip view grows a second band above the general-purpose lanes when it is active. Outputs gained a segment capacity and v/c table, which the chapter had no room for before `get_capacity` was bound, plus the GP and ML lane-group rows and a per-segment managed-lane table. Two Playwright pins cover them: Example Problem 4 through the UI moves Segment 11 period 1 from 4,499 veh/h at v/c 1.12 to 4,014 at the published 1.26 and back again when the panel is removed, which is a control on the panel rather than on the lane count alone, and Example Problem 5 through the UI prints the Exhibit 25-87 facility LOS row C/D/D/C/C with the Exhibit 25-81 ML capacity of 1,614 veh/h and the Exhibit 25-83 friction flag on the cells that carry it. The three page tests that predate this pass still pass unchanged.

**Every calculator form is inert until hydration (2026-08-13).** Each page server-renders a fully painted form whose fields already hold their defaults, and each field only becomes a live binding once `onMount` finishes loading the wasm module. Typing into that window wrote into a DOM the framework had not adopted, and on webkit the edit combined with the server-rendered value instead of replacing it, so a field seeded with 2000 could hold 20003800 and the analysis ran on twenty million veh/h without throwing. The Playwright specs never saw it because they have always gated on Calculate being enabled. Users have no such gate.

The guard is one attribute per page, `inert={!ready}` on the single `<form id="hcmNN">` each page already has, sixteen identical one-line changes. It was chosen over `disabled` on every input and over `readOnly`: `disabled` would mean roughly six hundred edits and a visible grey-out flash on every page load, `readOnly` does not cover selects or checkboxes, and `inert` blocks focus, pointer and editing for the whole subtree at the platform level with no styling of its own, so nothing flashes. It is supported in every browser the suite runs and degrades to today's behavior on anything older. Two tests pin it, on hcm12ml and hcm19, delaying the JS responses to hold the page in its server-rendered state (a warm preview server hydrates faster than a fill can land, so an ungated fill otherwise passes by luck): the field must still hold its default after an ungated fill, and must be editable again once Calculate enables.

**The webkit Chapter 11 fault is diagnosed and fixed on the page, and it is neither an engine matter nor an instantiation one (2026-08-13).** The note this replaces called it both, and the two Playwright retries it justified are now removed. Instantiation always succeeds. The fault is inside `wasmfreewayreliability_run` itself and arrives in two flavors, "Unreachable code should not be executed" and "Out of bounds memory access", leaving the page showing its error alert. Measured over 162 fresh webkit contexts driving the default-facility run one click each, 11 faulted, which is the 1-in-10 the earlier note reported. Chromium and firefox have never reproduced it.

The browser is at fault rather than the engine, and the determinism is what shows it. The module imports neither a clock nor a random source, the inputs are fixed page defaults, and every successful run allocated a byte-identical 1,376,256 bytes of linear memory and printed identical metrics. A computation that deterministic cannot fail 7% of the time on its own, so what varies is the runtime executing it. Only the first heavy run in a context is affected, which is the one run that transitions into WebKit's optimized wasm tier, and that is the same observation the earlier note read as cold instantiation. Faulted runs sometimes leave the module grown past a gigabyte, which is a consequence of the corrupted execution rather than its cause, since other faulted runs never grow at all.

The fix is `withWasmRetry` in `src/lib/wasmRetry.js`, used only on hcm11, the only page that drives hundreds of thousands of core-methodology iterations per click. It rebuilds and runs once more, and only on a `WebAssembly.RuntimeError`, so a mistyped weather or demand grid still surfaces immediately. The rebuild has to include the `WasmFacilitySegment` array and not only the facility, because the constructor takes ownership of each segment: a first attempt that retried without rebuilding them failed 12 of 160 contexts with "array contains a value of the wrong type", which is how that requirement was found. Over 160 fresh webkit contexts on the fix, 0 failed, the retry engaged in 8, and every recovered run printed the full pinned set of 240 scenarios, 960 observations, FFTT 2.28, mean TTI 1.264, median 1.097 and PTI 1.698. This is a user-facing fix rather than a test mitigation, since a Safari user previously got a failed analysis on roughly one first click in fifteen.

## Decision items for Rei

1. RESOLVED (verified 2026-08-08 against a fresh maturin build, 151 pytest green): the fix landed in the core during the pre-0.3.0 API work, so both bindings share it — `sut_percentage` defaults to 0 (general-terrain Exhibit 12-25) and PyO3 exposes the parameter and `e_t()` like WASM.
2. RESOLVED with item 1: the silent off-grid fallback no longer exists. An off-grid SUT mix (say 40%) raises an error naming Exhibits 12-26 through 12-28 instead of degrading to 1/(1 - p_t).
3. RESOLVED in middleware 0.3.7 (2026-08-11), and wider than the item as written. The four facility parameters are now trailing constructor arguments forwarded exactly as `WasmFreewayFacility` forwards them, so the fixture's interchange density of 0.8 reaches the internal facility; of the four, it is the only one that moves this fixture, because the core already defaults jam density to 190 pc/mi/ln and the queue-discharge drop to 7%. The same pass closed the two scenario-generation gaps the ch11 boundary header had recorded alongside it. `set_weather()` takes the Step B-6 inputs in the serde schema of the library's `WeatherInputs`, so the fixture's Exhibit 25-101 probabilities and Exhibit 25-102 durations pass verbatim, and `set_demand_multipliers()` takes the Equation 25-72 table as 12 rows of 7, so EP7 runs on its Exhibit 25-100 ratios instead of the Exhibit 11-18 national defaults. Both validate their shapes in the binding, because a short or transposed table falls back to a silent default in the core (all-zero weather probabilities, a demand multiplier of 1.0) rather than failing. Alongside them the boundary gained the readers it needed to pin the new inputs: `expected_weather_event_counts()`, `total_weather_events()`, `scenario_weather_event_counts()`, `seed_demand_multiplier()`, `tti_max()`, `pct_tti_above()`, and `on_time_pct_at_speed()`.

   **Follow-up done: the hcm11 page now exposes all of it, opt-in.** Three collapsed panels sit below Scenario Generation. Weather holds the 12-by-10 probability grid with the ten mean durations and the event DAF, Demand Multipliers holds the 12-by-7 table, and Facility Parameters holds the four trailing arguments, all four blank. Every one is inert while closed, so an untouched form still makes the same fifteen-argument, weather-free call and the existing page pins did not move (Reliability Rating 62.8%, mean TTI 1.264, median 1.097, PTI 1.698 on the default facility). Opening a panel seeds it from Example Problem 7 rather than from zeros, because an all-zero weather matrix is the weather-free run wearing a different hat.

   Reproducing the published example needed one thing beyond the three panels, and it is worth recording why. The Exhibit 25-104 metrics belong to Example Problem 7's own facility, not to this page's three-segment default, so the panels alone reproduce nothing. A **Load Example Problem 7** button now seeds the whole case: the Exhibit 25-99 demands over 12 periods, all eleven segments including the weaving segment (whose inputs the page had to expose, mirroring hcm10's weaving details card), the four facility parameters, the incident inputs, and both tables. The seeded run reproduces `tests/boundary/ch11_freeway_reliability.mjs` cell for cell at the page's three decimals: mean TTI 1.323 against a published 1.30, median 1.033 against 1.03, misery index 5.630 against 5.76, semi-standard deviation 1.963 against 2.05, and PTI 1.970 against a published 1.67, that last one being the documented Monte Carlo gap rather than a page defect. Two Playwright tests pin it, one on the seeded run and one control that switches every panel back off on the same geometry and requires the milder Exhibit 11-18 numbers to return (mean TTI 1.218, misery 3.257, rating 85.8%). The control is not decorative: it caught the page treating a cleared number input as still-filled, because Svelte writes `undefined` rather than the empty string into a value bound to `type="number"`, which would have sent NaN into the constructor.

   One input had to be added to make the comparison honest. The page has always run VMT-weighted, but Exhibit 25-104 reports Example Problem 7 probability-weighted over the 2,880 observations, which is the object the boundary file asserts against. The Facility Parameters panel now carries a weighting selector defaulting to VMT-weighted, so the old behavior and the old pins are untouched, and the Example Problem 7 loader selects probability-weighted. The reliability rating relabels itself to "share of observations" under that setting, since the HCM defines the rating on VMT and the VMT-weighted run gives 84.4% against a published 90.8%.
4. Promotion order out of beta once you have inspected each chapter.

   **Chapter 25 Example Problems 8 and 9 were checked against the chapter PDF, and they split (2026-08-13).** Both are reliability alternatives on the Example Problem 7 facility, and both are described in the book as modifications of "Example Problem 6", which is a book error: Exhibit 25-106 draws the EP1/EP7 facility with the added lane boxed on Segments 7-11, and the reliability inputs each inherits exist only in EP7.

   EP8 (reliability analysis with geometric improvements, Exhibits 25-106 and 25-107) is assertable and is now in the ch11 boundary file. It widens Segments 7-11 by a lane with the rng seed held fixed, so the scenario set is identical and the delta is purely geometric; `lanes` is the third positional argument of the segment constructor, so nothing new was needed on the surface. Twenty checks: the direction the Rust test `ep8_geometric_improvement_improves_reliability()` asserts, plus value pins with the Exhibit 25-107 published figure named beside each. Mean TTI 1.198 against a published 1.18, median 1.028 against 1.02, misery index 4.170 against 4.07, semi-standard deviation 1.526 against 1.71, TTI_max 36.73 against 33.5, %obs above TTI 2 at 2.19 against a published 1.42% of VMT, and the VMT-weighted reliability rating 95.5% against a published 97.56%. Widening the wrong five segments in a control run fails eleven of the twenty, so the block pins the geometry and not merely the fact that a run completes. Two published EP8 figures are not usable as targets and are not asserted as such: the narrative's baseline mean TTI of 1.54 is not the 1.30 of Exhibit 25-104, and the published PTI of 1.17 sits below the published mean of 1.18, which no right-skewed TTI distribution allows.

   EP9 (evaluation of incident management, Exhibit 25-108) was **not** assertable at the boundary when this note was written, and the gap was one input: it cuts every incident severity's mean duration and standard deviation by 30%, which the Rust test reaches by scaling `scenario_generation.incidents.duration_params` after the case loads, while the wasm constructor took incidents as exactly two arguments and wrapped them in an `IncidentInputs::default()`, leaving nothing to scale. **Closed in middleware 0.3.9 (2026-08-13)** by `set_incidents(config)` taking the serde shape of `IncidentInputs`, the way `set_weather()` takes `WeatherInputs`, with `clear_incidents()`, `has_incidents()`, and an `incident_duration_params()` readback. Two guards stood here and both did their job. The method probe failed on the first package built against 0.3.9, which is what forced the real block; and the second guard, which pinned the fixture's severity distribution to the library default the binding silently substituted, is retired into something stronger than a string comparison, since sending the fixture's whole incidents object through the setter now reproduces the two-argument constructor path cell for cell on the 2,880-cell TTI matrix.

   The EP9 block and its shape rejections are 40 checks, which with the two retired guards takes the file from 151 to 189. The duration parameters are read back from the binding rather than transcribed, and the Exhibit 11-22 defaults the constructor installs are asserted first (34.0/15.1, 34.6/13.8, 53.6/13.9, 67.9/21.9 twice, with the minima and maxima), so the 30% cut lands on the same numbers the core would use and the minima and maxima stay put, as in the Rust test. The direction is asserted as `ep9_incident_management_improves_reliability()` asserts it, on the identical draw of 186 incidents across the same 240 scenarios: VMT-weighted mean TTI 1.23777 to 1.21130, reliability rating 84.450% to 85.421%, misery index 3.5037 to 3.0869, TTI_max 39.71 to 32.35. Exhibit 25-108 publishes 1.35 to 1.20 mean TTI and 44.4 to 50.0 mi/h, and as with EP8 the published baseline is not the Exhibit 25-104 value for the same facility, so the published pair is named in the labels rather than asserted. Unlike the EP7 and EP8 pins these values are the library's own EP9 path computed natively on this fixture rather than book numbers, so they are pinned at 1e-9 where the measured wasm-versus-native gap is 2e-12. Running the same block at a 29% cut instead of 30% fails five of them, so the pins bind the modification and not merely a completed run. The remaining checks are the four shape rejections the binding owes the caller, since the core defaults all three list-shaped incident fields silently: a four-entry `duration_params` (which would restore the book duration for the severity it omits, the exact mistake this block invites), a four- or six-entry severity distribution, an eleven-month frequency table, and a config with no frequency source at all, which is what a wholly misspelled object deserializes into.
5. RESOLVED in middleware 0.3.6 (finding 3 above): `determine_facility_follower_density` is bound and both the hcm15 page and the ch15 boundary suite call it. The predicted D-to-C flip for a case3-shaped facility happened in the boundary suite, not on the page, because the page was already selecting the adjusted density and already printed 7.271 and LOS C. Two things left over. The published-value comparison for case4 rests on the LOS E of Exhibit 26-36; the 20.0 followers/mi quoted in the note this replaces was not traceable to an exhibit in the library tests, so it is not repeated. Middleware 0.3.6 was published to crates.io on 2026-08-11, tracking transportations_library 0.3.2, so the declared dependency now resolves for consumers.

6. Chapter 15 Step 11 passes the wrong argument for the Exhibit 15-6 band, and this predates 0.3.6. `determine_facility_los(fd, s_pl)` splits the LOS thresholds on posted speed limit (E above 12 followers/mi at 50 mi/h and up, above 15 below it), and `twolanehighways_test.rs` weights the posted limit by length to get that argument. Both `src/routes/hcm15/+page.svelte` and `tests/boundary/ch15_twolanehighways.mjs` pass the length-weighted *average speed* instead. All four fixtures post 55 mi/h, so the correct argument always selects the higher-speed band; case4's weighted average speed is 49.55 mi/h and selects the lower one, and its 19.897 is LOS E under both, which is why nothing has ever failed. A facility posting 55 with a follower density between 12 and 15 and an average speed under 50 would print D where the book says E. The fix is one argument in each of the two files, but it can move a printed letter, so it is left for you.

7. RESOLVED: middleware 0.3.7 published to crates.io 2026-08-12 (with 0.3.8 following the same day for the parclo surface, tracking library 0.3.3 on both registries). The staged pair that followed, middleware 0.3.9 (pedestrian mode + SPUI surface) on library 0.3.4, published 2026-08-13. REOPENED the same day: `HCM-middleware/Cargo.toml` now declares 0.3.10 for the mixed-flow surface, and neither it nor the library 0.3.5 beneath it has been published, so a clean clone no longer resolves. See the publish-gate paragraph under Full-suite status.

## Full-suite status

All seventeen test files pass, 3,320 checks executed (`npm run test:boundary`, re-measured 2026-08-14 after the mixed-flow and ch14 EP5 passes). Every row's count is the executed count measured from that run. The total counts only checks the harness actually ran: files whose wanted getters have no middleware wrapper yet skip those checks and print a NOTE saying so, and skips are not counted. As of 0.3.5 no file prints a NOTE or SKIP line, so the executed count and the written count are the same everywhere. The ch15 row grew from 160 to 168 in 0.3.6, two checks per fixture: the facility follower density itself, and the same value read back after a caller's own Step 9 loop, which is the ordering the binding has to survive. The ch11 row grew from 61 to 131 in 0.3.7, the whole of Example Problem 7 rather than the subset the binding could express, to 151 with the Example Problem 8 geometric-improvement alternative and the two guards recording why Example Problem 9 could not follow it, and to 189 in 0.3.9 when `set_incidents` retired both guards and Example Problem 9 replaced them. The seventeenth file is new in 0.3.10, `ch25_26_mixed_flow.mjs` at 88 checks, and the ch14 row grew from 51 to 75 on 2026-08-14; together they are the whole of the 3,208 to 3,320 difference. No other row moved, and no file prints a NOTE or SKIP line, so every row's written count is still its executed count.

**The committed `pkg/` is built against middleware 0.3.10 on transportations_library 0.3.5, and NEITHER IS ON crates.io yet.** A publish gate is therefore open again, after being closed since 0.3.9. Local builds resolve through the path entries in `HCM-middleware/Cargo.toml` and `cross-traffic-middleware/Cargo.toml`, so the committed `pkg/` and the 88 mixed-flow checks below are real, but a clean clone or a CI job that resolves from the registry will not build this state until the library publishes and then the middleware. Everything this file records other than the mixed-flow row was reproduced under 0.3.9 on library 0.3.4, both of which published 2026-08-13.

The ch10 row grew from 974 to 1,592 in this pass, and Chapter 10 is now complete at the boundary: all six of Chapter 25's Example Problems 1 through 6 run here, where before this pass EP4 carried only a binding guard and EP5 was absent entirely. Example Problem 4 contributes the five published matrices of Exhibits 25-71 through 25-76 plus the Exhibit 25-77 summary and the Equation 10-7/10-11/10-12 factors read on their own; Example Problem 5 contributes the six checks mirroring the Rust `ep5_*` tests; and Example Problem 3 gains the Exhibit 25-63 capacities, which had been checked only indirectly through the d/c matrix.

The ch14 row grew from 51 to 75 on 2026-08-14. The Rust header for `chapter14_integration.rs` claims Chapter 28 Example Problems 1 through 5, but EP5 had no boundary section, so the row read EP1-4 and the two layers disagreed about scope. EP5 now runs, though not the way the Rust test runs it, and the difference is recorded in the core-only list below rather than papered over. EP5 is the service-flow-rate and service-volume application, and the Rust test drives it through `ramp_service_flow_rate_ideal` and `ramp_service_volumes`, neither of which the middleware shim exports. What the boundary can do is run the same equation forward: feed each published service flow rate back in as demand under ideal conditions and assert the engine returns the Exhibit 14-3 threshold density the book solved for. That checks the published numbers of Exhibits 28-4 and 28-5 through the JS path, and it leaves the solver and the prevailing-condition SF/SV columns untested here.

Inverting the check inverts the tolerance, so the EP5 block does not use this file's usual +-0.5 pc/mi/ln density band, which at these flows would accept a service flow rate wrong by roughly 90 pc/h. The density response to demand was measured at 0.0327 pc/mi/ln per 6 pc/h of approaching freeway flow and 0.0220 per 3 pc/h of ramp flow, so the Rust tolerances map to +-0.033 and +-0.022 and the block uses those. At that tolerance the block also settles an internal inconsistency in Exhibit 28-5, whose SFI column prints 769 and 1,723 for LOS C and D while its own service-flow arithmetic in the next column multiplies 772 and 1,726 by 0.939. The engine agrees with the arithmetic column: 772 gives a density of 27.997 against the 28 threshold and 769 gives 27.973, outside the band. The Rust test already took 772 and 1,726, and this is independent support for that reading.

Chapter 25 Example Problem 10 has no boundary coverage. EP8 reached the boundary with the Chapter 11 re-anchoring and EP9 with middleware 0.3.9; EP10 (the deterministic planning-level method) sits in the reliability unit tests, so it is core-covered but unreached here.

Example Problem 11 is no longer a gap. The mixed-flow model landed in transportations_library 0.3.5 and was bound in middleware 0.3.10, and Ch.25 EP11 together with the mixed-flow half of Ch.26 EP5 now runs at the boundary in `tests/boundary/ch25_26_mixed_flow.mjs`, 88 checks at the tolerances of `chapter25_composite_grade_integration.rs` and `chapter12_integration.rs`. Both examples reproduce end to end: EP5 at CAF_mix 0.7345, C_mix 1,726.2 veh/h/ln, FFS_mix 60.14, BP_mix 0, calibration speeds 37.53 and 44.38, S_mix 47.42 and D_mix 31.63; EP11 at per-segment capacities 1,874.5 / 1,932.8 / 1,747.5 veh/h/ln governed by the 1 mi 5% grade, segment speeds 57.69 / 58.77 / 48.12 mi/h, travel times summing to 290.93 s and an overall 55.68 mi/h, plus the Exhibit 25-109/110/111 speed triples by vehicle class.

Three of those published values are asserted at the self-consistent number rather than the printed one, each labelled as a book defect at the check that pins it, following the core: EP5's density is 31.7 from its own Step 8 and not the 32.6 of the comparison paragraph, EP11's Segment 2 rate is 61.3 and not the 62.6 printed one line above the division that uses 61.3, and EP11's travel times sum to 291.5 and not the 294 of the Step 7 prose. A fourth known defect, the `+ 1` of Equation 26-16, is deliberately NOT corrected in either layer, because the printed form drives BP_mix to zero here and the example rationalises that zero in words, and BP_mix feeds the equations that produce the published speed and density.

**This surface is boundary-validated but not page-exposed.** There is no `hcm25` or mixed-flow route, no diagram and no report section; `WasmMixedFlow` and `WasmCompositeGrade` are reachable only from the boundary suite. Whether the model gets its own page, joins hcm12 behind a method selector, or stays a library-and-middleware surface is a separate decision and not a validation gap. Two things belong in that decision when it is made. The first is that the truck-performance curves are digitised only for the grades and speeds the two worked examples need, so a page would have to constrain its inputs to the -5/0/2/3/5% grades and the digitised speeds rather than accept a free-form grade; anything else throws by name, and the suite asserts two of those refusals reach JS carrying the exhibit that would have to be digitised. The second is that above mixed-flow capacity the method reports no speed at all, and `serde_wasm_bindgen` crosses that absence as `undefined` rather than `null`, so a page guarding on `null` would render the oversaturated case as a speed.

## Known out-of-scope at the boundary (core-tested only, no WASM path)

- Ch.12: `estimate_number_of_lanes` design step (Ch.26 EP2's 3-lane answer is core-only).
- Ch.14: the ramp service-flow-rate solver. `ramp_service_flow_rate_ideal`, `ramp_service_volumes`, and the `ServiceDemandBasis` enum they take (`ApproachingFreeway { ramp_fraction }` and `FixedFreeway { v_f }`) are all public in `hcm::merge_diverge::merge_diverge` but are free functions the middleware shim does not re-export, so `pkg/` has no wrapper for them and the Chapter 28 EP5 search cannot be called from JS. The boundary covers EP5 by running the equation forward instead, as described under full-suite status, which reaches the published service flow rates but not the solver that finds them and not the prevailing-condition SF and SV columns, since those need `ramp_service_volumes`. Binding the two functions plus a serde form of the basis enum would close this; nothing else about EP5 is out of reach.
- Ch.15: BicycleLOS.
- Ch.19: actuated timing estimation and RTOR volume estimation.
- Ch.23: closed by middleware 0.3.2. `WasmAlternativeIntersection` plus the EDTT/offset helper functions cover the RCUT (EP13), MUT (EP15), and DLT offset (EP16) paths, and the boundary suite runs them (100 of the chapter's 257 checks, the other 157 being the Part B interchanges; the "200 checks" recorded here previously did not match any measured subtotal). EP14 (RCUT with signals) landed at the boundary 2026-08-09 and EP12 (RCUT with merges) now runs as a full worked journey, its per-movement results taken from the EP12 prose because the chapter publishes them there rather than in a table. Still core-only: nothing.
- Ch.10: closed. Managed-lane facilities were closed by middleware 0.3.6 and the work zone and segment capacities by 0.3.7, so all three former entries here are gone and the chapter has no core-only Chapter 25 EP1-EP6 test left. `WasmManagedLaneFacility` takes ml_case1.json verbatim and the six Rust `ep5_*` tests all have boundary equivalents. `WasmFacilitySegment.set_work_zone()` carries case4.json's Segment 11 work zone through, so the five Rust `ep4_*` tests do too; the guard that used to sit here (Segment 11 period-1 d/c pinned at 1.121, the published 1.26 over exactly the CAF_wz of 0.892) failed the moment the binding landed, which is what it was for, and the real matrices replaced it. `WasmFreewayFacility.get_capacity()` now asserts Exhibits 25-63 and 25-71 directly rather than indirectly through the d/c matrices. Still core-only: nothing.
- Ch.11: work zones and special events on the scenario generator, and with them the two Chapter 37 ATDM direction-of-effect tests (the shoulder-lane strategy and merge ramp metering), which reach the core through `WorkZoneEvent` constructors the wasm surface does not expose. Weather, the Exhibit 25-100 demand ratios, and the four facility parameters left this list in middleware 0.3.7, Example Problem 8 with the re-anchoring that followed, and the incident inputs, and with them Example Problem 9, in middleware 0.3.9.
- Ch.18: closed by middleware 0.3.3. All three Equation 18-7 access-point delay sources are now reachable (the `access_point_delays_s` published-input hook, the computed Chapter 30 §4 procedure via `add_access_point`, and the Exhibit 18-13 planning path with the fixture's own turn percentages), as are the Step 2 intermediates S_0, f_CS, f_A, f_pk, f_L, and f_v. EP1's published running time (33.54 s) and travel speed (23.67 mi/h) now reproduce at the boundary. The pre-0.3.3 forced-planning default path is retained as a labeled regression anchor so existing 28-argument callers cannot shift silently. Still core-only: nothing.
- Ch.16: closed by middleware 0.3.3. `add_segment_summary` + `aggregate()` express the Exhibit 16-7 "HCM method output" path, so Ch.29 EP1 EB and WB now run at the boundary (facility base FFS 40.1, LOS C, poorest-segment LOS D all exact), and the sixteen new `add_segment` arguments make the full Ch.30 EP1 geometry expressible (facility base FFS 40.78 and travel speed 23.67, previously unreachable). `analyze()` refusing to run on a summary-built facility is asserted, not just documented. Remaining deviation, not a binding gap: EP1's published facility travel speeds (22.6 EB / 22.2 WB) do not reproduce because the fixtures copy Segments 1 and 5 into the unpublished Segments 2-4, giving 22.1 / 21.5. The fixtures say so in their own `_source` notes and the Rust core tests carry the same gap.
- Ch.17: largely closed by middleware 0.3.3. Monthly snowfall, `jan1_day_of_week`, facility `prop_left_turn_lanes`, boundary-signal k/I/approach lanes, `add_atdm_strategy`, and `num_oversaturated_scenarios` are all expressible, so the boundary now reproduces the Rust test's computed point values rather than only landing inside its bands (mean TTI 1.5449, TTI-80 1.5927, PTI 1.7462, reliability rating 98.83, oversaturated scenarios 70 exact), and Ch.29 EP5 Strategy 1 plus the Ch.37 §5 adaptive signal control strategy are both evaluated. The pre-0.3.3 call shape is retained as a labeled regression anchor under the old wide bands. Still core-only: per-scenario results, which the boundary exposes only through summary getters plus the oversaturated count, so a Rust-style filter over `scenario_results` has no equivalent here. `AtdmStrategy` also reaches the binding through its serde form only, so `AtdmStrategy::adaptive_signal_control` is expressed as the `sat_flow_adjustment` value it computes (1 / (1 - 0.135) = 1.15607) rather than called by name.

## Coverage gaps (no fixture or Rust test at any layer)

- Ch.26 later EPs (multilane-highway LOS, service volumes). EP5's mixed-flow half left this list in middleware 0.3.10; the remaining mixed-flow applications later in the chapter are still uncovered at any layer.
- Ch.27 later EPs and Ch.28 EPs after 5 (design applications). Ch.28 EP5, the service-volume application, left this list on 2026-08-14; its remaining boundary limits are in the core-only section above, not here.
- Ch.31 §10 EPs 2+ (pedestrian and bicycle modes at signals).
- Ch.32 TWSC EP2 (pedestrian crossing).
- Ch.34 EPs 2-4, 6-11, 17.
- Ch.15: which published two-lane EPs the four fixtures correspond to is undocumented in the Rust tests.
